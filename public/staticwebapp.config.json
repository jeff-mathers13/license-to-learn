{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/assets/*", "/*.{png,jpg,jpeg,svg,ico,json,webmanifest}"]
  },
  "routes": [
    { "route": "/api/*", "allowedRoles": ["authenticated"] }
  ]
}
